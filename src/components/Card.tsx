import React from 'react';



const Card: React.FC<any> = ({cart, setTitle, carts}) => {


  return (
    <div style={{width:`${cart.width}px`, backgroundColor: 'red', height: '50px'}}>

    </div>
  )
};

export default Card;
